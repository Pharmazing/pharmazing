import { createStyleSheet } from 'react-native-unistyles';

export const editAddressFormStyles = createStyleSheet((theme) => ({
  container: {
    // flex: 1,
    paddingTop: 16,
    // borderWidth: 1,
  },
}));
